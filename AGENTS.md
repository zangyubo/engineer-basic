# AGENTS.md

本文件用于向 AI 编码助手（如 Codex、Cursor、Claude Code 等）说明本仓库的工作约定，AI 助手在仓库内执行操作前应先阅读本文件。

## 项目简介

本仓库是 **注册电气工程师基础考试** 的复习项目，用于记录复习笔记、刷题与进度管理。项目说明详见 `README.md`。

## 对 AI 助手的硬性要求

### 提交规则（最高优先级，必须遵守）

- **未经用户明确允许，禁止执行任何 `git commit` 提交操作。**
- 仅当用户明确命令提交（例如"提交"、"commit"）时，才允许执行 `git commit`。
- 未经用户要求，同样不要主动执行 `git push` 等发布类操作。
- `git add`（暂存文件）可以正常执行，但提交必须等待用户明确指令。

### 操作范围（文件边界）

- AI 助手的文件操作范围限定在**本项目目录**（仓库根目录及其子目录）之内。
- 对项目目录之外的任何文件，**禁止一切危险操作**：删除、覆盖、移动、重命名、批量写入、修改内容等一律不允许。
- 确需只读查看项目外文件时，须先向用户说明；无论何种情况，都不得修改项目外文件。

### 其他约定

- `docs/` 目录存放本地复习资料与笔记，已被 `.gitignore` 忽略，不入库，AI 助手不应将其加入版本控制。

### 脚手架与示例代码

- 需要脚手架代码或示例代码时，统一放入**对应章节目录下的 `code/` 子目录**（例如 `公共基础考试/01-数学/code/`），**不要放在项目根目录**。
- 每个代码文件头部必须添加必要的注释：说明代码用途、对应的章节或知识点、创建日期。

### 产物与临时文件

- 工作过程中产生的截图、预览自检等临时产物，统一放入项目根目录下的 `dist/` 目录，**不要散落在项目根目录或各章节目录中**。
- `dist/` 已被 `.gitignore` 忽略，不入库。
